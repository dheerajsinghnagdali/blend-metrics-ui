import { Meta } from "@storybook/react";
import { DatePicker } from "./date-picker";

const meta: Meta = {
  title: "DatePicker"
};

export default meta;

export const Default = () => {
  return <DatePicker />;
};
